import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { login, register } from '../../api/auth'
import { useAuth } from '../../context/AuthContext'
const destination = (role) => role === 'doctor' ? '/doctor' : '/patient'
export default function Signup() { const [form, setForm] = useState({ fullName: '', email: '', password: '', phone: '', role: 'patient', doctorName: '', specialty: '' }), [error, setError] = useState(''), [loading, setLoading] = useState(false); const { refreshUser } = useAuth(); const navigate = useNavigate(); const set = (key, value) => setForm({ ...form, [key]: value }); async function submit(e) { e.preventDefault(); setLoading(true); setError(''); try { await register(form.fullName, form.email, form.password, form.role, form.phone, form.doctorName, form.specialty); await login(form.email, form.password); const user = await refreshUser(); navigate(destination(user?.role)) } catch (err) { const data = err.response?.data; setError(data?.detail || Object.values(data || {})[0]?.[0] || 'Could not create your account.') } finally { setLoading(false) } } return <div className="flex min-h-screen items-center justify-center bg-paper p-5"><form onSubmit={submit} className="w-full max-w-md border border-ink/15 bg-panel p-7 sm:p-9"><p className="font-display text-3xl font-bold">HospitalMS</p><h1 className="mt-7 font-display text-3xl font-bold">Create an account</h1><p className="mt-2 text-sm text-ink/60">Patient and doctor registration is available here.</p>{error && <p className="mt-5 border-l-4 border-status-alert bg-status-alert/10 p-3 text-sm">{error}</p>}<label className="mt-6 block text-sm font-semibold">Full name<input required value={form.fullName} onChange={(e) => set('fullName', e.target.value)} className="mt-2 w-full border border-ink/25 p-3 font-normal" /></label><label className="mt-5 block text-sm font-semibold">Email<input required type="email" value={form.email} onChange={(e) => set('email', e.target.value)} className="mt-2 w-full border border-ink/25 p-3 font-normal" /></label><label className="mt-5 block text-sm font-semibold">Phone<input required value={form.phone} onChange={(e) => set('phone', e.target.value)} className="mt-2 w-full border border-ink/25 p-3 font-normal" /></label><label className="mt-5 block text-sm font-semibold">Password<input required minLength="8" type="password" value={form.password} onChange={(e) => set('password', e.target.value)} className="mt-2 w-full border border-ink/25 p-3 font-normal" /></label><label className="mt-5 block text-sm font-semibold">I am registering as<select value={form.role} onChange={(e) => set('role', e.target.value)} className="mt-2 w-full border border-ink/25 p-3 font-normal"><option value="patient">Patient</option><option value="doctor">Doctor</option></select></label>{form.role === 'doctor' && <><label className="mt-5 block text-sm font-semibold">Doctor name<input required value={form.doctorName} onChange={(e) => set('doctorName', e.target.value)} className="mt-2 w-full border border-ink/25 p-3 font-normal" /></label><label className="mt-5 block text-sm font-semibold">Specialty<input required value={form.specialty} onChange={(e) => set('specialty', e.target.value)} className="mt-2 w-full border border-ink/25 p-3 font-normal" /></label></>}<button disabled={loading} className="mt-7 w-full bg-ink py-3 text-sm font-bold text-panel disabled:opacity-50">{loading ? 'Creating account...' : 'Create account'}</button><p className="mt-6 text-center text-sm text-ink/65">Already registered? <Link to="/login" className="font-semibold underline">Sign in</Link></p></form></div> }
